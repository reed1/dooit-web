export interface DooitSchema {
  name: string;
  label: string;
}

export interface Workspace {
  id: number;
  description: string;
  order_index: number;
}

export interface Todo {
  id: number;
  description: string;
  ord: number;
}

